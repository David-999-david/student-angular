// import { inject } from '@angular/core';
// import { CanActivateChild, CanActivateChildFn, CanMatchFn } from '@angular/router';
// import { AuthGuard } from './auth-guard';

// export const authCanMath: CanMatchFn = (route, segments) =>
//   inject(AuthGuard).canMatch(route, segments);

// export const authCanActivateChild: CanActivateChildFn = (route, state) =>
//   inject(AuthGuard).canActivateChild(route, state);
